target: trace
input:
  qasm_text: "OPENQASM 3; qubit[1] q;"
  solver_config: solvers/qutip_default.yaml
  hardware_config: hardware/transmon_default.yaml
output:
  out_dir: runs/template_task
  persist_artifacts: true
  artifact_mode: targeted
  export_plots: false
  export_dxf: false
