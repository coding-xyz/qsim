# 开发进度

本页记录当前文档和配置体系里最值得注意的状态。

## 当前口径

- `task` 使用扁平写法
- `device` 使用组件化结构
- `pulse` 使用结构化通道、波形和操作映射
- `solver` 使用结构化求解器写法
- `analyser` 使用独立配置文件，所有派生结果都从 `trajectory` 计算

## 对使用者的建议

- 新任务直接从仓库模板起步
- 优先保持五类配置分工清晰
- 先做最小可运行示例，再逐步加复杂度
- 文档只保留当前推荐写法
